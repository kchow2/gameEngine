package guis;

import renderEngine.Loader;

public class GuiPanel extends GuiComponent{

	public GuiPanel(int guiID, Loader loader, GuiPosition guiPosition, GuiTexture texture) {
		super(guiID, loader, guiPosition, texture);
	}
}
